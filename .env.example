# Shopify Nano-SRE Environment Configuration
# Copy this file to .env and update with your values

# ============================================
# STORE CONFIGURATION
# ============================================

# Required: Your Shopify store URL
STORE_URL=https://your-store.myshopify.com

# Optional: Shopify Admin API key (required for Shopify Doctor skill)
# Get from: Shopify Admin > Settings > Apps and integrations > Develop apps
SHOPIFY_ADMIN_API_KEY=

# ============================================
# LLM CONFIGURATION
# ============================================

# LLM Provider (openai, anthropic, or ollama)
# - openai: Uses OpenAI API (requires LLM_API_KEY)
# - anthropic: Uses Claude API (requires LLM_API_KEY)
# - ollama: Uses local Ollama (no API key needed)
LLM_PROVIDER=openai

# API key for the LLM provider (optional for ollama)
LLM_API_KEY=

# Model name for LLM (depends on provider)
# - OpenAI: gpt-4, gpt-4-turbo, gpt-3.5-turbo
# - Anthropic: claude-3-opus, claude-3-sonnet, claude-3-haiku
# - Ollama: mistral, llama2, etc.
LLM_MODEL=gpt-4

# ============================================
# ALERTING
# ============================================

# Optional: Webhook URL for alerts (Discord, Slack, etc.)
# Example Discord: https://discordapp.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN
# Example Slack: https://hooks.slack.com/services/YOUR_WEBHOOK_DATA
ALERT_WEBHOOK_URL=

# ============================================
# MONITORING & STORAGE
# ============================================

# Interval in minutes between checks (default: 30)
CHECK_INTERVAL_MINUTES=30

# Path to SQLite database file (default: ./nano_sre.db)
SQLITE_DB_PATH=./nano_sre.db

# ============================================
# PRIVACY & SECURITY
# ============================================

# Enable PII redaction in logs and reports (default: false)
REDACT_PII=false

# Enable blurring of sensitive selectors in screenshots (default: false)
# When enabled, elements matching PII patterns will be blurred
SCREENSHOT_BLUR_ENABLED=false
